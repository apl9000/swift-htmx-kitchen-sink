#extend("layout"):
  #export("body"):
    <h2 class="text-xl font-bold mb-4">You have #(count) todos</h2>
    <ul class="space-y-2">
      #if(todos.count > 0):
        #for(todo in todos):
          <li class="bg-white p-2 rounded shadow">#(todo.title)</li>
        #endfor
      #else:
        <li class="text-gray-500">Nothing to do yet.</li>
      #endif
    </ul>
  #endexport
#endextend